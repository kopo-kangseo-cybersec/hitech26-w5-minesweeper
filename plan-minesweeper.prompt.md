## Plan: Vanilla JS 지뢰찾기

9x9 보드와 지뢰 10개를 사용하는 지뢰찾기를 `index.html`, `style.css`, `script.js` 세 파일로 구현한다. 첫 좌클릭은 안전하게 처리하고, 별도 빌드 도구 없이 브라우저에서 직접 실행·검증한다.

**Steps**
1. `index.html`에 제목, 상태 메시지, 남은 지뢰 수, 타이머, 재시작 버튼, 보드 컨테이너를 배치하고 외부 CSS/JS를 연결한다. 보드 셀은 JavaScript가 생성하며 접근 가능한 버튼으로 구성한다.
2. `script.js`에 게임 상태를 구현한다. 초기화 시 빈 9x9 보드를 만들고, 첫 유효 좌클릭 뒤 클릭 칸을 제외한 위치에 지뢰 10개를 배치한다. 인접 지뢰 수 계산과 빈 칸 자동 확장을 포함한다.
3. 좌클릭 공개, 우클릭 깃발 토글, 남은 지뢰 수 갱신, 타이머 시작·정지를 연결한다. 지뢰 클릭 시 패배, 모든 안전 칸 공개 시 승리로 종료하고 종료 후 입력을 차단한다.
4. `style.css`에 보드와 상태별 셀 스타일을 작성한다. 닫힌 칸, 공개 칸, 숫자, 깃발, 지뢰, 폭발, 승리 상태를 구분하고 작은 화면에서도 9x9 보드가 유지되도록 반응형 제약과 포커스 스타일을 적용한다.
5. 브라우저에서 `index.html`을 실행해 첫 클릭 안전, 공개/연쇄 공개, 우클릭 깃발, 카운터, 타이머, 승패, 재시작, 종료 후 입력 차단을 확인한다. 콘솔 오류와 좁은 화면 레이아웃도 확인한다.

**Relevant files**
- [guide.md](d:/repo/hitech26-w5-minesweeper/guide.md) — 필수 기능과 9x9·10개 예시의 기준.
- [index.html](d:/repo/hitech26-w5-minesweeper/index.html) — 신규 화면 구조.
- [style.css](d:/repo/hitech26-w5-minesweeper/style.css) — 신규 보드 및 상태 스타일.
- [script.js](d:/repo/hitech26-w5-minesweeper/script.js) — 신규 게임 로직.

**Verification**
1. 브라우저에서 `index.html`을 열어 CSS/JS가 정상 연결되는지 확인한다.
2. 첫 좌클릭 칸에 지뢰가 없고, 우클릭으로 깃발을 꽂고 해제할 때 카운터가 `10`에서 정확히 변하는지 확인한다.
3. 지뢰 클릭 패배, 안전 칸 전체 공개 승리, 두 상태에서 타이머 정지와 입력 차단을 확인한다.
4. 재시작 시 새 보드, 타이머 `0`, 남은 지뢰 수 `10`, 초기 상태 메시지가 복원되는지 확인한다.
5. 모바일 폭 화면과 브라우저 콘솔에서 레이아웃·오류를 확인한다.

**Decisions**
- 구현 범위는 사용자가 요청한 HTML/CSS/Vanilla JS 3파일과 핵심 게임 기능으로 제한한다.
- 보드는 9x9, 지뢰는 10개로 고정한다.
- 첫 좌클릭 칸은 안전하다. 첫 클릭 시점에 지뢰를 배치해 이를 보장한다.
- 난이도 선택, 저장, 사운드, 백엔드, 프레임워크, 빌드 도구, PRD/README 개편은 이번 구현 범위에서 제외한다.
- 별도 테스트 러너가 없으므로 브라우저 수동 검증을 기본으로 한다.
