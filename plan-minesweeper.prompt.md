## Plan: Vanilla JS 지뢰찾기

PRD에 정의된 Windows 95 스타일 지뢰찾기를 `index.html`, `style.css`, `script.js`로 구현한다. 별도 빌드 도구 없이 브라우저에서 실행하며, 세 난이도와 마우스 조작을 지원한다.

**Steps**
1. `index.html`에 난이도 선택, 3자리 지뢰 카운터, 최대 999초 타이머, 상태 이모지 재시작 버튼, 상태 메시지, 보드 컨테이너를 배치한다.
2. `script.js`에서 초급 9 x 9/10개, 중급 16 x 16/40개, 고급 30 x 16/99개의 설정을 중앙 관리한다.
3. 첫 번째 공개 칸과 주변 8칸을 제외한 위치에 지뢰를 배치하고, 인접 지뢰 수 계산과 빈 칸 연쇄 공개를 구현한다.
4. 좌클릭 공개, 우클릭 깃발, 중클릭 및 좌우 양손 chording을 처리한다. 정확한 수의 깃발이 있을 때만 주변 닫힌 칸을 연다.
5. 지뢰 클릭 시 모든 지뢰를 공개하고 폭발 칸을 강조한다. 안전 칸을 모두 열면 지뢰를 자동 표시하고 컨페티를 실행한다.
6. `style.css`에서 회색 베젤, 입체 테두리, LED 디스플레이, 상태별 셀, 고급 보드 가로 스크롤을 구성한다.
7. 브라우저에서 첫 클릭 안전, 연쇄 공개, 깃발, chording, 승패, 타이머 상한, 재시작, 난이도 전환, 좁은 화면을 확인한다.

**Relevant files**
- [index.html](index.html) — 게임 화면과 접근성 구조
- [style.css](style.css) — 클래식 Windows 스타일과 반응형 보드
- [script.js](script.js) — 게임 상태, 보드 생성, 입력과 승패 로직
- [prd.md](prd.md) — 기능 요구사항 원문

**Verification**
1. 초기 상태에서 난이도별 카운터가 `010`, `040`, `099`, 타이머가 `000`, 이모지가 `😊`인지 확인한다.
2. 모서리·가장자리·중앙 첫 클릭에서 클릭 칸과 주변 8칸이 안전한지 확인한다.
3. 숫자보다 적은 깃발로 chording할 때 변화가 없고, 정확한 깃발 수일 때 주변 칸이 열리는지 확인한다.
4. 잘못된 깃발로 chording하거나 지뢰를 직접 클릭했을 때 `😵`, 전체 지뢰 공개, 폭발 강조가 나타나는지 확인한다.
5. 모든 안전 칸을 열었을 때 `😎`, 자동 깃발, 카운터 `000`, 컨페티, 타이머 정지를 확인한다.
6. 타이머가 `999`에서 멈추고 `1000`으로 넘어가지 않는지 확인한다.
7. 재시작과 난이도 변경 후 보드, 카운터, 타이머, 컨페티, 상태가 초기화되는지 확인한다.

**Decisions**
- Vanilla JavaScript와 정적 HTML/CSS만 사용한다.
- 첫 클릭 전 깃발은 허용하되, 실제 첫 공개 시 지뢰를 배치한다.
- 저장, 사운드, 백엔드, 프레임워크, 빌드 도구는 범위에서 제외한다.
- 별도 테스트 러너가 없으므로 브라우저 수동 검증을 기본으로 한다.