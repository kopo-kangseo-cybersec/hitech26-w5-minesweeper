# 지뢰찾기 실습 가이드

같은 지뢰찾기 게임을 **두 가지 방식**으로 만들어보고 비교합니다.

> AI에게 말로 요청 → AI가 계획을 세움 (Plan)
>
> 내가 먼저 요구사항을 문서로 정리 → 그 문서를 AI에게 줌 → AI가 그걸 바탕으로 계획을 세움 (PRD + Plan)

## 브랜치 구조

| 브랜치 | 용도 |
|---|---|
| `main` | Fork 직후 원본 그대로 (건드리지 않음) |
| `freeform` | PRD 없이, 여러 줄 프롬프트로 바로 Agent에게 구현 요청한 버전 |
| `prd` | PRD 작성 → Plan 모드로 확인·보완 → Agent로 구현한 버전 |
| `gh-pages` | 최종 선택한 버전을 배포하는 브랜치 (`prd` 기준으로 생성) |

## 진행 순서

1. 이 저장소를 Fork → 내 컴퓨터로 Clone

2. **`freeform` 브랜치 생성**
   ```
   git checkout -b freeform
   ```
   여러 줄 프롬프트로 Agent에게 바로 지뢰찾기 구현을 요청하세요. 완성되면 로컬에서 실행해보고 확인만 합니다 (배포하지 않습니다). 커밋은 해두세요.

3. `main`으로 돌아가서 **`prd` 브랜치 생성**
   ```
   git checkout main
   git checkout -b prd
   ```

4. 아래 PRD 템플릿을 채워 문서를 작성하세요 (저장소에 `PRD.md` 파일로 저장하는 것을 권장합니다).

5. Copilot Chat에 작성한 PRD를 붙여넣으며 "이 PRD를 바탕으로 구현 계획을 세워줘"라고 요청하세요.

6. Plan 모드가 세운 계획이 PRD 내용을 빠짐없이 반영했는지 확인하세요. 빠진 부분이 있으면 계획 보완을 요청하세요.

7. 계획이 만족스러우면 Implement plan으로 Agent에게 구현을 맡기세요. 완성되면 로컬에서 실행해보고 확인합니다. 커밋해두세요.

8. **두 버전을 로컬에서 나란히 비교**해보세요. (탭 두 개로 각각 열거나, 순서대로 실행)

9. 더 마음에 드는 브랜치를 `main`에 병합하세요. (기본적으로 `prd`를 권장합니다)
   ```
   git checkout main
   git merge prd
   ```

10. **완성된 게임을 소개하는 `README.md`를 작성**하세요. 저장소에 있는 `README.md`는 지금은 뼈대(섹션 제목)만 있는 빈 파일입니다. 배포하기 직전인 지금, 이 게임이 무슨 게임인지, 두 버전을 비교했을 때 무엇이 달랐는지, 최종적으로 어떤 버전을 선택했는지 직접 채워 넣으세요.

11. 병합된 `main`에서 `gh-pages` 브랜치를 생성해 GitHub Pages로 배포하세요.
    ```
    git checkout -b gh-pages
    git push origin gh-pages
    ```

12. **`prd` 브랜치 기준**으로 원본 저장소(`kopo-kangseo-cybersec/hitech26-w5-minesweeper`)의 `main`에 Pull Request를 보내세요. PR 템플릿의 항목을 채워서 제출하세요.

> 💡 `freeform` 브랜치는 삭제하지 마세요. "이런 방식도 시도해봤다"는 기록이 남아 나중에 회고할 때 유용합니다.

## PRD 템플릿 (최소 구성)

```markdown
# 지뢰찾기 게임 PRD

## 목표
(어떤 게임인지 한 줄로)

## 핵심 규칙
- 격자 크기:
- 지뢰 개수:
- 좌클릭 동작:
- 우클릭(깃발) 동작:
- 승리 조건:
- 패배 조건:
- 첫 클릭 안전 규칙 (첫 클릭한 칸엔 지뢰가 없어야 하는지):

## 화면 요소
- 타이머:
- 남은 지뢰 수 표시:
- 재시작 버튼:

## 이번엔 만들지 않을 것 (선택)
- (예: 난이도 선택 기능은 이번엔 제외)
```

## 프롬프트 예시

```
[Copilot Agent 프롬프트 — freeform 버전]
좌클릭하면 칸을 열고, 우클릭하면 깃발을 꽂는 지뢰찾기 게임을 만들어줘.
9x9 격자에 지뢰 10개, 타이머와 남은 지뢰 수를 보여줘.
Vanilla JS로 만들고 파일 하나(index.html)에 담아줘.
```

```
[Copilot Chat 프롬프트 — PRD 전달]
아래 PRD를 바탕으로 지뢰찾기 게임을 구현하려고 해.
코드를 바로 짜지 말고, 이 PRD의 내용을 빠짐없이 반영한
구현 계획부터 세워줘.

[여기에 작성한 PRD 붙여넣기]
```

```
[Copilot Chat 프롬프트 — 계획 보완]
계획에 "첫 클릭 안전 규칙"이 빠진 것 같아. 추가해서 다시 세워줘.
```

## 이해 단계 체크포인트

- [ ] PRD 없이 만들 때와 PRD를 먼저 쓰고 만들 때, 결과물이 어떻게 달라졌는지 설명할 수 있다
- [ ] PRD → Plan → Agent로 이어지는 흐름을 순서대로 설명할 수 있다
- [ ] 브랜치를 병합(merge)한다는 것이 무엇인지, 왜 지금 이 시점에 병합했는지 말할 수 있다

## 커스터마이징 범위

자유: 격자 크기, 지뢰 개수, 디자인, 난이도 선택 기능 추가 여부
필수 포함: 좌클릭/우클릭 기본 동작, 승리·패배 조건, 재시작 기능
