# 지뢰찾기(Minesweeper) 게임 PRD

## 목표

기존 Windows 지뢰찾기와 동일한 핵심 플레이 경험을 제공하는 웹 기반 지뢰찾기 게임을 구현한다.

---

## 개발 구조

프로젝트는 아래와 같이 파일을 분리하여 개발한다.

```text
project/
├── index.html
├── style.css
├── script.js
└── README.md
```

- HTML: 게임 UI 구조 담당
- CSS: 디자인 및 레이아웃 담당
- JavaScript: 게임 로직 담당

---

## 게임 모드

사용자는 게임 시작 전 또는 재시작 시 난이도를 선택할 수 있다.

### 초급 (Beginner)

- 격자 크기: 9 × 9
- 지뢰 개수: 10개

### 중급 (Intermediate)

- 격자 크기: 16 × 16
- 지뢰 개수: 40개

### 고급 (Expert)

- 격자 크기: 30 × 16
- 지뢰 개수: 99개

### 난이도 선택 기능

- 드롭다운 또는 버튼 그룹으로 구현
- 난이도 변경 시 게임 자동 초기화
- 새로운 지뢰 배치 생성

---

## 핵심 규칙

### 지뢰 배치

- 게임 시작 시 지뢰를 무작위 배치
- 첫 클릭 위치 및 주변 8칸에는 지뢰를 배치하지 않는다.

### 좌클릭 동작

#### 닫힌 칸 클릭

- 지뢰가 없는 경우:
  - 칸을 공개한다.
  - 주변 지뢰 개수를 표시한다.

- 지뢰인 경우:
  - 게임 오버 처리
  - 모든 지뢰 표시

#### 주변 지뢰 수가 0인 경우

- 인접한 빈 칸들을 자동으로 연쇄 공개한다.
- BFS 또는 DFS 방식 사용 가능

### 우클릭 동작

기존 지뢰찾기와 동일하게 동작한다.

반복 순서:

```text
닫힘
↓
깃발
↓
물음표
↓
닫힘
```

#### 깃발 상태

- 지뢰로 예상되는 위치 표시
- 좌클릭 불가
- 남은 지뢰 수 계산에 반영

#### 물음표 상태

- 확실하지 않은 위치 표시
- 좌클릭 가능

### 숫자 칸 기능 (선택 구현)

기존 지뢰찾기의 편의 기능 구현

조건

- 열린 숫자 칸 주변의 깃발 개수 == 숫자 값

동작

- 숫자 칸 좌클릭 시
- 주변의 닫힌 칸 자동 공개

예시

```text
숫자 3
주변 깃발 3개

→ 나머지 칸 자동 오픈
```

---

## 승리 조건

다음 조건을 만족하면 승리한다.

- 지뢰가 아닌 모든 칸이 공개됨

또는

- 모든 지뢰 위치에 깃발이 설치되고
- 나머지 안전 칸이 공개됨

승리 시

```text
🎉 YOU WIN!
```

메시지 표시

---

## 패배 조건

다음 조건 발생 시 즉시 종료

- 지뢰 칸 좌클릭

패배 시

- 모든 지뢰 위치 공개
- 잘못된 깃발 표시
- 추가 입력 비활성화

메시지

```text
💥 GAME OVER
```

---

## 화면 구성

### 상단 정보 패널

포함 요소

#### 난이도 선택

- 초급
- 중급
- 고급

#### 남은 지뢰 수

표시 방식

```text
💣 10
```

#### 타이머

표시 방식

```text
⏱ 125
```

- 첫 클릭 시 시작
- 초 단위 증가

#### 재시작 버튼

표시 예시

```text
😊
```

상태 변화

```text
😊 진행 중
😮 클릭 중
😵 패배
😎 승리
```

버튼 클릭 시

- 게임 완전 초기화

---

## 게임 보드

각 셀은 아래 상태 중 하나를 가진다.

### 닫힌 셀

```text
□
```

### 열린 셀

```text
1
2
3
...
```

숫자별 색상 적용

| 숫자 | 색상 |
|--------|--------|
| 1 | 파랑 |
| 2 | 초록 |
| 3 | 빨강 |
| 4 | 남색 |
| 5 | 갈색 |
| 6 | 청록 |
| 7 | 검정 |
| 8 | 회색 |

### 깃발

```text
🚩
```

### 물음표

```text
❓
```

### 지뢰

```text
💣
```

---

## UI/UX 요구사항

### 디자인

- 클래식 Windows 지뢰찾기 스타일
- 명확한 입체 버튼 효과
- 셀 상태 시각적 구분

### 반응성

- 데스크톱 우선 개발
- 브라우저 크기에 맞게 중앙 정렬

### 접근성

- 키보드 조작은 필수 아님
- 클릭이 명확하게 인식되어야 함

---

## 게임 상태 관리

아래 상태를 관리한다.

### Playing

```text
게임 진행 중
```

### Win

```text
승리
```

### Lose

```text
패배
```

게임 종료 후

- 추가 입력 금지
- 재시작만 가능

---

## JavaScript 구현 요구사항

### 데이터 구조

각 셀은 아래 정보를 가진다.

```javascript
{
  row,
  col,
  isMine,
  isOpen,
  isFlagged,
  isQuestion,
  adjacentMines
}
```

### 주요 함수

```javascript
initGame()
createBoard()
placeMines()
calculateNumbers()

openCell()
toggleFlag()
expandEmptyCells()

checkWin()
gameOver()
restartGame()

startTimer()
stopTimer()
updateMineCounter()
```

---

## 이번 버전에서 구현 제외

- 온라인 랭킹
- 로그인 기능
- 최고 기록 저장
- 다크 모드
- 커스텀 맵 제작
- 모바일 롱터치 최적화
- 멀티플레이

---

## 완료 기준 (Definition of Done)

다음 조건을 모두 만족하면 개발 완료로 판단한다.

- 초급/중급/고급 난이도 선택 가능
- 첫 클릭 안전 보장
- 좌클릭/우클릭 기능 정상 작동
- 깃발 및 물음표 순환 기능 구현
- 빈 칸 자동 확장 구현
- 승리/패배 판정 구현
- 타이머 구현
- 남은 지뢰 수 계산 구현
- 재시작 기능 구현
- HTML/CSS/JS 분리
- 브라우저 단독 실행 가능
- 콘솔 오류 없음